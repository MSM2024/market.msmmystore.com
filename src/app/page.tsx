'use client'

import ElianaExperience from "@/components/zafiro101/ElianaExperience"

// ENTRYPOINT público de ZAFIRO (zafiro.msmmystore.com).
// Raíz = Entrada Soberana (diamante ELIANA + constelación de portales).
// La interfaz legacy (feed social) quedó archivada en /inicio.
export default function Home() {
  return <ElianaExperience />
}